namespace CatA.A8;

// A8 (file 2 of 2): the implementing half of the partial class/method. Same doc-comment ID as the
// defining declaration in Part1, so it is the same graph node -> alive via the ConfigureServices root.
public partial class Sample
{
    public partial void PartialMethod() { }
}
