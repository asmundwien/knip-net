<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <NoWarn>CS0067;CS0169;CS0649;IDE0051;IDE0052</NoWarn>
    <!-- L18 (collision row, HUMAN DECISION 2026-07-15 §6): pins the reordered confidence rule where C2
         (publicApi) precedes C4 (deleteCodeAndTests). Three test-only findings in production mode:
           PublicTestOnly   (public)   -> publicApi hazard -> C2. unconfigured=low / configured=medium.
           InternalTestOnly (internal) -> no publicApi hazard -> falls through to C4 -> medium.
         Not part of Knip.slnx; compiled at runtime by MSBuildWorkspace. -->
  </PropertyGroup>

</Project>
